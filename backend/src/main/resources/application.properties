
# server.address=0.0.0.0
# spring.application.name=backend
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# # Schema management: Flyway owns DDL; Hibernate validates only.
# spring.jpa.hibernate.ddl-auto=validate
# spring.flyway.enabled=true
# spring.flyway.locations=classpath:db/migration

# # Safety when adopting Flyway on an existing DB that already has tables:
# # - empty DB: migrations run normally
# # - non-empty DB: Flyway baselines to V1 to avoid duplicate-table creation errors
# spring.flyway.baseline-on-migrate=true
# spring.flyway.baseline-version=1
# spring.flyway.validate-on-migrate=true

# spring.flyway.schemas=public
# spring.flyway.default-schema=public
# spring.jpa.properties.hibernate.default_schema=public

spring.application.name=backend

# Schema (GLOBAL — must be same everywhere)
spring.flyway.schemas=public
spring.flyway.default-schema=public
spring.jpa.properties.hibernate.default_schema=public