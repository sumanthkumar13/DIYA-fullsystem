# server.port=8081
# spring.datasource.url=jdbc:postgresql://localhost:5432/diya_db
# spring.datasource.username=diya_user
# spring.datasource.password=diya123
# spring.datasource.driver-class-name=org.postgresql.Driver

# # DEV SAFETY: Flyway owns schema; Hibernate validates only
# spring.jpa.hibernate.ddl-auto=validate
# spring.flyway.enabled=true
# spring.flyway.locations=classpath:db/migration
# spring.flyway.baseline-on-migrate=true
# spring.flyway.baseline-version=1
# spring.flyway.validate-on-migrate=true
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true

server.port=8081
server.address=0.0.0.0

# Local DB
spring.datasource.url=jdbc:postgresql://localhost:5432/diya_db
spring.datasource.username=diya_user
spring.datasource.password=diya123
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate (DO NOT CHANGE)
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=1
spring.flyway.validate-on-migrate=true

# Debug (dev only)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true